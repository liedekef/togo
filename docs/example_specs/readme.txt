This dir contains the different scripts a rpm can contain. They are empty, so easy to copy and change to your needs.
The file rpmspec.txt explains which script gets called in which scenario.
The file systemd.txt talks about some systemd specific stuff for rpm.
